import './filter.css';

const Filter = () => {
    return (
        <div className="three-buttons">
            <button>All</button>
            <button>Active</button>
            <button>Done</button>
        </div>
    );
};

export default Filter;