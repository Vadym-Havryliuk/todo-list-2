import './header.css';

const Header = () => {
    return (
        <div class="header">
            <h1>Todo List</h1>
            <span>3 more to do, 0 done</span>
        </div>
    );
};

export default Header;